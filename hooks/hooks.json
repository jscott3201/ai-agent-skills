{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "bash ${CLAUDE_PLUGIN_ROOT}/hooks/session-start.sh"
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "if": "Bash(git push*)",
            "command": "echo 'Agent wants to push to remote. Approve to allow.' >&2; exit 1"
          },
          {
            "type": "command",
            "if": "Bash(git reset --hard*)",
            "command": "echo 'Blocked: git reset --hard is destructive. Use a safer alternative or ask the user.' >&2; exit 2"
          },
          {
            "type": "command",
            "if": "Bash(git checkout -- *)",
            "command": "echo 'Blocked: git checkout -- discards uncommitted changes. Ask the user first.' >&2; exit 2"
          },
          {
            "type": "command",
            "if": "Bash(git clean -f*)",
            "command": "echo 'Blocked: git clean -f removes untracked files permanently. Ask the user first.' >&2; exit 2"
          },
          {
            "type": "command",
            "if": "Bash(rm -rf *)",
            "command": "echo 'Blocked: rm -rf is destructive. Use a targeted rm or ask the user.' >&2; exit 2"
          }
        ]
      }
    ],
    "TeammateIdle": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "echo '{\"additionalContext\": \"Before going idle, check the task list for pending unblocked tasks. If any exist, claim and start the next one.\"}'"
          }
        ]
      }
    ],
    "Notification": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "osascript -e 'display notification \"Claude Code needs your attention\" with title \"Claude Code\"'"
          }
        ]
      }
    ]
  }
}
