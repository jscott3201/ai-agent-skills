{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "if": "Bash(git push*)",
            "command": "echo 'Blocked: git push is not allowed. Only the user pushes to remote.' >&2; exit 2"
          },
          {
            "type": "command",
            "if": "Bash(git reset --hard*)",
            "command": "echo 'Blocked: git reset --hard is destructive. Use a safer alternative or ask the user.' >&2; exit 2"
          },
          {
            "type": "command",
            "if": "Bash(git checkout -- *)",
            "command": "echo 'Blocked: git checkout -- discards uncommitted changes. Ask the user first.' >&2; exit 2"
          },
          {
            "type": "command",
            "if": "Bash(git clean -f*)",
            "command": "echo 'Blocked: git clean -f removes untracked files permanently. Ask the user first.' >&2; exit 2"
          },
          {
            "type": "command",
            "if": "Bash(rm -rf *)",
            "command": "echo 'Blocked: rm -rf is destructive. Use a targeted rm or ask the user.' >&2; exit 2"
          }
        ]
      }
    ],
    "TeammateIdle": [
      {
        "hooks": [
          {
            "type": "prompt",
            "prompt": "A teammate is about to go idle. Check: are there pending, unblocked tasks in the shared task list that this teammate could claim? If yes, respond with {\"ok\": false, \"reason\": \"There are pending tasks available. Check the task list and claim the next unblocked task before going idle.\"}. If no pending tasks exist or all remaining tasks are blocked, respond with {\"ok\": true}."
          }
        ]
      }
    ],
    "Notification": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "osascript -e 'display notification \"Claude Code needs your attention\" with title \"Claude Code\"'"
          }
        ]
      }
    ]
  }
}
