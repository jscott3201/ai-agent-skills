#!/usr/bin/env bash
# Inject skill routing table and session continuity instructions at session start.

cat <<'ROUTE'
{"additionalContext": "justin-tools: 38 skills available. Suggest manual skills when you see the user working on a matching task.\n\nPlanning: feature-design, research, debate\nBuilding: rust-scaffold, error-catalog, modularize, refactor, ci-pipeline\nTesting: test-strategy (auto), rust-ci-check, plan-verify (auto), requirements-trace, sequential-bench (auto)\nDebugging: debug (auto), perf-profile, incident-response\nReviewing: deep-review (auto), safety-checks (auto), code-standards (auto), dep-audit (auto)\nReleasing: release-prep, migration-guide, migration-safety\nTracking: milestone-tracking (auto), deferred-tracking (auto), notes (auto), session-tracker (auto)\nDocs/Maintenance: docs-sync (auto), api-doc-gen, env-audit, crate-health, project-onboard\nBackground: commit-workflow, subagent-dispatch, team-coordination, technical-writing, no-shortcuts (auto)\n\nSession continuity: If gql_query tool is available, silently query for recent session context before the first substantive task:\n  MATCH (s:Session) WHERE s.project = '<project>' AND s.summary IS NOT NULL RETURN s.skill, s.summary, s.next_steps, s.outcome, s.date ORDER BY s.date DESC LIMIT 3\nIf results exist, briefly note what was worked on recently. Do not present this as a formal report — just weave it naturally into your awareness. If gql_query is not available, check for _agentskills/SESSION_LOG.md instead.\n\nFor detailed routing: /justin-tools:skill-guide"}
ROUTE
